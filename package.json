{
  "name": "hack-media-verify",
  "version": "1.0.0",
  "description": "Verify media has not been tampered with from the edge to the cloud.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "hash": "ts-node src/main.ts hash",
    "verify": "ts-node src/main.ts verify",
    "ledger": "ts-node src/ledger.ts",
    "fixtures:fetch": "bash scripts/fetch-fixtures.sh",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/hash.test.js dist-test/test/merkle.test.js dist-test/test/chunks.test.js dist-test/test/fileMerkle.test.js dist-test/test/timing.test.js",
    "bench": "tsc -p tsconfig.test.json && node dist-test/test/bench.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
